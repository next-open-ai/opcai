{
  "name": "@opcai/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup",
    "start": "node dist/main.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.0",
    "@opcai/agent-core": "workspace:*",
    "@opcai/contracts": "workspace:*",
    "fastify": "^5.3.0"
  },
  "devDependencies": { "tsup": "^8.5.0" }
}
